# DLSSG Manager v1.7.4

**内嵌上游 Mod 从 0.3.2 升级到 0.3.4**（跳过有已知崩溃的 0.3.3）。

---

## 上游 0.3.3 / 0.3.4 带来了什么

### 0.3.3：架构改写提前到启动 + 修复 0.3.2 闪烁

- **向游戏改写显卡架构的动作提前到游戏启动时，并改报 RTX 50**。
  使用 Streamline 2.8 的游戏（如《最终幻想 7 重生》）此前在启动阶段就判定
  「本显卡不支持 DLSS-G」并卸掉帧生成插件（issue #509 / #528）；
  本版在那之前就装好改写，游戏内的 3X / 4X / 6X 选项也按 RTX 50 放开。
- `Optimized=1` 不再默认跳过同一组内重复的真实帧拷贝
  （新增可选键 `SkipRepeatedRealCopy`，**默认关**）。
  该项从未在实际游戏中验证，0.3.2 上有画面闪烁报告（issue #532）。
  **档位 1 画面仍与官方逐位一致**。

### 0.3.4：修复 0.3.3 在 RTX 30 上的崩溃

- 修复 0.3.3 在 RTX 30 上的崩溃 —— 启动或开启 DLSS 后显卡驱动重置
  （《极限竞速：地平线 6》表现为 FHC01；issue #535 / #538 / #540 / #542）。
- 原因：NVIDIA App 的 DLSS 覆盖或 NGX 在线更新生效时，**DLSS 超分模型也收到了
  本项目给 Streamline 的显卡架构改写**，在 RTX 30 上走了不属于这张卡的路径，GPU 挂起。
- 现在 **NVIDIA 自己的组件一律得到真实架构**，改写只对 Streamline 和游戏生效。

**因此直接跳过 0.3.3（有已知 RTX 30 崩溃），从 0.3.2 一步升到 0.3.4。**

---

## 本工具的相应变更

- **payload/ 12 个代理 DLL 全部更新到 0.3.4**，SHA-256 逐个核对与上游仓库一致。
  （310.9：30,011,168 B / 310.1：27,986,208 B）
- **兼容升级**：新增 0.3.2 哈希表作为「上一版」、0.3.1 表继续保留 ——
  用 v1.7.0~v1.7.3 部署过的游戏可直接「重新部署 / 更新配置」覆盖，
  不会被误判为第三方文件。
- **INI 格式零变更**：功能键逐行比对与上游 0.3.4 出厂文件完全一致
  （`Optimized` / `MaxGeneratedFrames` / `Preset` / `Level` 等），
  旧配置无需改动即可继续使用。
- **显卡伪装说明补充**：上游 0.3.3+ 已在启动时改报 RTX 50（NVAPI/Streamline 层），
  与本工具的显卡伪装（系统设备实例名称）机制不同、互不冲突；
  对「按型号拒绝开启」的游戏现在优先靠上游改写，伪装作为补充。
- 使用说明补充 `SkipRepeatedRealCopy` 键与上游 0.3.3+ 架构改写说明。

---

## 兼容性说明

| 项目 | 结论 |
|---|---|
| 已部署 0.3.2 的游戏 | 可直接覆盖升级（哈希表已识别），无需先恢复 |
| 已部署 0.3.1 / 0.3.0 的游戏 | 同样可直接覆盖（历史哈希表保留） |
| dlssg_sm86.ini | 无需改动，功能键与 0.3.4 完全一致 |
| INI 新键 `SkipRepeatedRealCopy` | 默认关、不出厂；仅在需要时手动加 `1` |
| RTX 30 用户 | **应升级** —— 0.3.3 有已知崩溃，0.3.4 已修复 |
| 配置兼容性 | 无破坏性变更 |

---

## 校验值

| 文件 | 大小 |
|---|---|
| `DLSSG_Manager_1.7.4_setup_x64.exe` | 103.1 MB |
| `DLSSG_Manager_1.7.4_portable_x64.zip` | 142.6 MB |

内嵌上游 Mod：**DLSSG SM86 0.3.4**（12 个代理 DLL 与上游逐位一致）。

---

**全部核心能力来自上游 [sdli1995/dlssg_for_sm86](https://github.com/sdli1995/dlssg_for_sm86)，请先去给上游一个 Star。**
