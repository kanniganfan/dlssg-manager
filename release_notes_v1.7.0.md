# DLSSG Manager v1.7.0

同步上游 **DLSSG SM86 0.3.2**（2026-09-17）。本版核心是**推理内核重写**与
**一致性档位（Optimized 0–3）**，界面已跟进这一新语义。

---

## 上游 0.3.2 带来了什么

### 1. 推理内核重写 —— 画面与官方逐位一致，且更快

310.9 构建中新增的 **26 个推理内核镜像全部重写**：生成画面现在与官方
NVIDIA DLSS-G **完全一致**（上游在 RTX 3080 Ti 与 RTX 5070 上实测逐位相同），
**不再有损**，同时还有小幅加速（3080 Ti 上 0–8%）。

> 0.3.1 及更早的「优化内核」在图像层面是有损的，只是肉眼看不太出来。
> 0.3.2 把这一点彻底修掉了。

### 2. `Optimized` 由开关改为四级一致性档位

以前只有 `0`（原厂）和 `1`（优化）两态。0.3.2 改成 **0–3 四级**，判据只有一条：
**允许生成的画面偏离官方运行库多远**。

| 档位 | 含义 | 画面一致性 |
|---|---|---|
| `0` | 原厂内核，不做任何加速 | 与官方运行库逐位一致 |
| `1` | 全部「可证明不改变输出」的加速 | **与官方运行库逐位一致**（默认） |
| `2` | 档位 1 + 有损图像内核 | 不再逐位一致；对官方输出 PSNR ≈50 dB 以上 |
| `3` | 全部有损加速 | 不再逐位一致；画质代价最大，最快 |

档位累加且单调 —— 更高档位只会再加东西，不会撤回低档位的加速。

**⚠️ 重要限制**：档位 2/3 依赖 310.9 构建里新增的有损图像内核。**310.1 构建没有
这些内核，上游会把 2/3 静默当作 1 处理**。本工具因此在选择 310.1 运行库时
直接收起这两个档位，不给出一个实际不生效的选项。

### 3. 容错行为改进

- **INI 单键写错不再关掉整个 Mod**：该键回退到自己的默认值并记一条
  `configuration_warning`（`Level=1` 就能看到），其余键照常工作。
- **档位值非法时回退到档位 1**（而非 0）—— 写这个键的人本意是要打开加速，
  不该被静默退回原厂。
- 后端不支持的开关改为「剔除后继续安装」并记
  `kernel_selection_unsupported{stripped_flags}`，而不是整个拒绝。

### 4. RTX 20 系（真 Turing）已完成实机验证

0.3.1 只恢复了 RTX 20 的可用性，但**从未在真机上验证过**（此前只有 RTX 3070 上
的 `.target sm_75` 前向 JIT 结果）。0.3.2 补齐了这一环：RTX 2080 Ti 实机上
`sm_75` cubin 全族创建并运行、**0 回退**，cubin 与 PTX 逐位一致，档位 0 与档位 1
逐位一致，游戏内可用。

### 5. 12 个代理 DLL 全部重编译重签名

体积继续增长（310.9：29,975,840 B / 310.1：27,951,392 B）。签名证书与指纹不变
（`CN=DLSSG for SM86 (self-signed)`，SHA-1 `85BA66762F851E49148D706915D09026281418E6`）。

---

## 本工具（DLSSG Manager）的相应更新

- **新增「一致性档位」选择器**：位于「插帧配置」卡片，直接给出 `0 / 1 / 2 / 3`
  四档，选中后在右侧显示该档含义（档位 ≥2 会明确标注「不再是逐位一致，画质有损」）。
- **档位随运行库联动**：切到 310.1 时档位自动收起为 `0 / 1`，并提示
  「310.1 无有损内核，仅支持档位 0–1」。
- **部署日志与 INI 说明同步更新**：日志会写出实际生效的档位，INI 头部完整
  解释四个档位并标注 2/3 仅在 310.9 有效。
- **payload 更新到 0.3.2**：12 个 DLL 与上游逐位一致（SHA-256 全表已更新）。
- **兼容升级**：保留 0.3.0 / 0.3.1 的哈希表，用旧版部署过的游戏可以直接
  「重新部署 / 更新配置」覆盖，不会被误判为第三方文件。
- 多语言词典同步扩充（中 / 繁 / EN 三语 241 条，零缺翻）。

---

## 兼容性说明

- **配置文件向后兼容**：老 INI 里的 `OptimizedKernels=1` 仍然等价于档位 1
  （上游保留了它作为 `Optimized` 的别名，且同样接受 0–3）。
- **出厂 `MaxGeneratedFrames` 仍为 `3`（4X）**，与 0.3.1 一致，本版未改动；
  要 6X 需在 310.9 运行库下手动选 6X 档位。
- **无破坏性变更**：本工具旧版部署过的游戏目录可直接覆盖升级。

## 已知情况

- 档位 2/3 在 310.1 运行库下不生效（上游行为，本工具已提前收起该选项）。
- 上游对 RTX 20 系的画质与性能**尚未量化** —— 0.3.2 只证明帧生成能在 20 系上
  跑起来（数千次 Evaluate 零失败、零回退），没有 FPS 数据，也没有与 30 系的画质对比。

---

## 校验值

| 文件 | 大小 |
|---|---|
| `DLSSG_Manager_1.7.0_setup_x64.exe` | 102.7 MB |
| `DLSSG_Manager_1.7.0_portable_x64.zip` | 142.2 MB |

`payload/version.dll`（310.9）SHA-256：
`39b16f2cdb16450f0edc0e0b14231951e10954131ad9aa12037700e18dc91040`

---

**全部核心能力来自上游 [sdli1995/dlssg_for_sm86](https://github.com/sdli1995/dlssg_for_sm86)，请先去给上游一个 Star。**
