fix: payload 多路径查找 + 报错附诊断（v1.5.3）

问题：构建产物 dist/ 里的 exe 旁边没有 payload，直接双击运行会报
「未找到运行包目录，无法部署」，且报错不提示程序找了哪些位置，难以自查。

修复：
- 新增 payload_search_paths()：按优先级列出全部候选位置
  （exe旁/payload、exe旁、上一级/payload、上一级/dlssg_sm86_pack、
  %LOCALAPPDATA%/Programs/DLSSG Manager/payload），去重保序
- payload_dir() 改为遍历该列表，任一命中即用
- verify_payload() 未找到时附带「已查找位置」列表，把哑报错变成可自查

版本 1.5.3