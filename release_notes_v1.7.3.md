# DLSSG Manager v1.7.3

**自述文档与界面截图全面更新**，同时修掉两个排查截图时发现的界面缺陷。

---

## 一、自述文档（README）重写

- **全部截图换成 v1.7.3 实拍**：旧截图还停留在 1.0.0 时代，里面是已经删掉的
  「显卡路由 / 入口 DLL（winhttp）/ 采样模式」等控件 —— 现在换成当前真实界面。
- 新增 4 张配图（收进可折叠的「更多截图」里，不打扰首屏）：
  **一致性档位**（310.9 四档 / 310.1 两档对比）、**English** 界面、**窄窗口**布局。
- 内容同步当前能力：一致性档位 0–3、6X、RTX 20 系已由上游 0.3.2 实机验证、
  更新检测、`scripts/` 里的自检脚本清单。
- 补回被我重写时弄丢的**「致谢与出处」**整节（上游署名不能少）。

## 二、使用说明更新

- 「配置项说明」整节重写 —— 原来还在讲已经删掉的 `显卡路由 / KernelImage /
  采样模式 / winhttp`，现在对齐为 **运行库 / 入口 DLL / 插帧倍率(2–6X) / 一致性档位 / 诊断日志**。
- 新增「四·五、显卡伪装」完整说明（此前完全没写）。
- 「怎么用」第 3 步补充选档位与倍率；「界面与交互」补充更新徽标与页脚按钮；
  「恢复与卸载」补上更新检测缓存文件路径。

## 三、顺带修复的两个界面缺陷

排查截图时逐像素检查发现：

1. **详情页末尾的操作提示文字被裁掉一半**。
   根因：`QScrollArea` 配 `setWidgetResizable(True)` 时会把页面压到视口高度，
   而 `QVBoxLayout` 对开了 `wordWrap` 的 `QLabel` 算不出真实内容高度
   （`QLabel.minimumSizeHint` 只按一行算），于是整页最小高度被低估到 65px，
   布局便放心地把 697px 的内容压进 441px 的视口，末尾文字被裁。
   修复：新增 `PageBox` 容器，宽度变化时把「按当前宽度换行后的真实内容高度」
   同步成最小高度 —— 内容放不下就出现滚动条，而不是裁掉。
2. **一致性档位说明折成 3 行且断在词中间**（`（不再` / `是逐位一致，画质有` / `损）`），
   且与档位标签重复写「有损」。改为说明文字单独占一整行，措辞去掉重复。

---

## 校验值

| 文件 | 大小 |
|---|---|
| `DLSSG_Manager_1.7.3_setup_x64.exe` | 102.9 MB |
| `DLSSG_Manager_1.7.3_portable_x64.zip` | 142.4 MB |

内嵌上游 Mod：**DLSSG SM86 0.3.2**（12 个代理 DLL 与上游逐位一致）。

---

**全部核心能力来自上游 [sdli1995/dlssg_for_sm86](https://github.com/sdli1995/dlssg_for_sm86)，请先去给上游一个 Star。**
