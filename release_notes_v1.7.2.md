# DLSSG Manager v1.7.2

修复 v1.7.1 引入的**页脚按钮中文被裁掉一半**的显示缺陷，并按反馈**移除「检测上游」逻辑**。

---

## 一、问题定位

**位置**：主窗口**页脚**（底部状态条）右侧的两个按钮 —— 「检查更新」与「上游仓库」。

**现象**：中文字符上下各被切掉一截，看起来像缺笔画（例如「检查更新」的下半部分没了）。

**触发场景**：任何窗口尺寸、任何语言下都会出现，与分辨率、缩放无关 ——
只要按钮被创建出来就是坏的。

---

## 二、原因分析

用截图像素测量 + Qt 布局实测，定位到**单一根因**（不是 overflow、不是 z-index、不是绝对定位）：

```
按钮固定高度        setFixedHeight(24)          = 24px
QSS 上下 padding    padding: 8px 15px           = 16px
边框                1px × 2                     =  2px
──────────────────────────────────────────────────────
留给文字的高度      24 - 16 - 2                 =  6px
文字实际需要        QFontMetrics.height()       = 12px
```

**6px 装 12px 的字 → 上下各裁掉 3px。**

从原始截图逐行测亮度也印证了这一点：字形只占 y=17~22，正好 **6px**。

逐项排除用户列出的其它可能原因：

| 候选原因 | 是否成立 | 说明 |
|---|---|---|
| 容器尺寸不足 | ❌ | 页脚有充足空间，按钮自身高度才是约束 |
| overflow 隐藏 / 裁剪 | ❌ | Qt 无此 CSS 概念；QPushButton 内部按 contentRect 绘制 |
| 层级 / z-index 覆盖 | ❌ | 无控件叠压，文字是被**自己的内容区**裁的 |
| 行高与字体设置 | ⚠️ 次要 | 字体本身正常（12px 行高），问题在可用高度不足 |
| **内边距 / 外边距** | ✅ **主因** | **QSS 8px 上下 padding 吃掉了 16px** |
| 绝对定位 | ❌ | 页脚是 QHBoxLayout，无绝对定位 |
| 文本截断与换行 | ❌ | 水平方向未截断，问题在垂直方向 |

**为什么只有这两个按钮坏**：其它 `#ghost` 按钮用的是 `setFixedHeight(32)`，
32-18=14px ≥ 12px，刚好够；页脚按钮为了塞进窄条被写成 24px，越过了临界点。

---

## 三、修复方案

1. **新增紧凑按钮样式 `#ghostSm`**：把上下 padding 从 8px 压到 **4px**，
   占用从 18px 降到 10px —— 这是为窄条区域专门准备的样式，不污染其它按钮。
2. **去掉 `setFixedHeight`**，改用样式驱动的高度：Qt 的 `sizeHint()` 会自动把
   字体行高 + padding + 边框算进去，**高 DPI 缩放放大字体时也不会裁字**。
3. **页脚「上游仓库」→「项目主页」**，指向本项目仓库（配合下面的移除需求）。
4. 修掉顺带发现的**提示浮层（toast）盖住页脚按钮**的老问题，并抽成可测的纯函数。

修复后实测：字形高度 **6px → 11px**，按钮高度 24px（固定）→ 25px（自适应）。

---

## 四、兼容性

- **100% / 125% / 150% / 175% 四种缩放下逐一验证**，`sizeHint` 始终 ≥ 字体行高。
- **5 种窗口尺寸**（1160×840 / 900×700 / 820×560 / 780×520 / 1400×620）下页脚按钮
  高度稳定为 25px，无裁剪、无重叠。
- 新增**运行时审计脚本**：遍历界面上全部 `QPushButton`（4 种缩放下共 132 个实例），
  断言「按钮可用高度 ≥ 文字所需高度」。已用**注入旧缺陷**的方式反向验证过该审计
  确实能捕获此问题（注入后 4 种缩放全部报错）。

---

## 五、按要求移除「检测上游」

只保留**软件自身**的更新检测：

- `core.py`：删除 `UPSTREAM_REPO` / `UPSTREAM_API` / `UPSTREAM_PAGE` 常量与
  `UpdateInfo.upstream_version` / `upstream_newer` 字段，`check_update()` 不再请求上游。
- `ui.py`：删除「上游仓库」按钮与 `open_upstream_page()`，改为「项目主页」+ `open_repo_page()`。
- i18n：清理全部上游检测文案（中/繁/EN 三语零缺翻，267 条）。
- 现在检查更新**只发一个请求**（本项目 `releases/latest`），行为更简单可预期。

---

## 校验值

| 文件 | 大小 |
|---|---|
| `DLSSG_Manager_1.7.2_setup_x64.exe` | 102.9 MB |
| `DLSSG_Manager_1.7.2_portable_x64.zip` | 142.4 MB |

内嵌上游 Mod：**DLSSG SM86 0.3.2**（12 个代理 DLL 与上游逐位一致）。

---

**全部核心能力来自上游 [sdli1995/dlssg_for_sm86](https://github.com/sdli1995/dlssg_for_sm86)，请先去给上游一个 Star。**
