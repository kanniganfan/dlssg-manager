# DLSSG Manager v1.7.1

本次新增**更新检测**：软件会自己看有没有新版本，不用再手动来仓库翻。

---

## 新增：更新检测

### 怎么用

- **自动**：启动后约 2.5 秒做一次静默检查，**6 小时内只查一次**（结果缓存在本地，
  不会每次开软件都发请求）。
- **手动**：页脚右侧新增 **「检查更新」** 按钮，随时可点。
- **发现新版本时**：标题栏右侧出现蓝色徽标 **「有新版本 vX.Y.Z」**，点击直达下载页。

### 同时会检查上游 Mod

除了本工具，还会顺带查一下上游 [dlssg_for_sm86](https://github.com/sdli1995/dlssg_for_sm86)
有没有新版本（本版内嵌 **0.3.2**）。上游更新时会在日志里提示，并给出上游仓库入口
（页脚 **「上游仓库」** 按钮）。

### 关于隐私

这次改动让软件**首次产生了联网行为**，所以在此明确说明：

- 检查是**只读的 GitHub 公开 API 查询**（`api.github.com`）；
- **不上传任何本机数据**，不发送任何标识信息（无设备 ID、无游戏列表、无遥测）；
- **检查失败不影响任何功能** —— 离线、被墙、超时都只是日志里记一条提示，
  静默检查失败时甚至不会弹任何提示打扰你。

README 与《使用说明》里原先写的「本工具不联网」已相应更正为
「除更新检测外不联网」，避免表述与实际行为不符。

---

## 顺带修复

- **提示浮层遮住页脚按钮**：新增页脚按钮后发现，弹出提示（toast）会盖住
  「检查更新 / 上游仓库」。已改为浮在页脚**上方**，并加了回归断言防止复发。

---

## 其他

- 多语言词典扩到 **269 条**，简体中文 / 繁體中文 / English **零缺翻**（新增 28 条
  全部与更新检测相关的文案）。
- 命令行也支持：`DLSSG Manager.exe --update`（`--no-upstream` 可跳过上游检查）。

---

## 校验值

| 文件 | 大小 |
|---|---|
| `DLSSG_Manager_1.7.1_setup_x64.exe` | 102.9 MB |
| `DLSSG_Manager_1.7.1_portable_x64.zip` | 142.4 MB |

内嵌上游 Mod：**DLSSG SM86 0.3.2**（12 个代理 DLL 与上游逐位一致）。

---

**全部核心能力来自上游 [sdli1995/dlssg_for_sm86](https://github.com/sdli1995/dlssg_for_sm86)，请先去给上游一个 Star。**
